import * as React from 'react';
import style from './[FCName].module.css';

interface [FCName]Props {};

export const [FCName] = ({}: [FCName]Props) => {
    return (<div>[FCName]</div>);
};

[FCName].displayName = "[FCName]";
